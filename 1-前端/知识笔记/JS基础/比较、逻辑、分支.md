# 比较、逻辑、分支

## 比较运算符

   - >： 左边是否大于右边

   - <： 左边是否小于右边

   - >=： 左边是否大于或等于右边

   - <=： 左边是否小于或等于右边

   - ==： 左右两边是否相等

   - ===： 左右两边是否类型和值都相等

   - != 不相等

   - !==： 左右两边是否不全等

- 比较结果为boolean类型，即只会得到true或false

## 比较运算符的细节

- 字符串比较，是比较的字符对应的ASCII码

   - 从左往右依次比较


   - 如果第一位一样再比较第二位，以此类推

   - 比较的少，了解即可

- NaN不等于任何值，包括它本身

   - 用isNaN函数判断是否为NaN

- 尽量不要比较小数相等或全等的问题，因为小数有精度问题

- 不同类型之间比较会发生隐式转换

   - 最终把数据隐式转换转成number类型再比较

   - 所以开发中，如果进行准确的比较我们更喜欢 === 或者 !==

**==> picture [454 x 296] intentionally omitted <==**

## 逻辑运算符的使用

逻辑运算符：

**==> picture [467 x 109] intentionally omitted <==**

## 逻辑运算符里的短路

短路：只存在于 && 和 || 中，当满足一定条件会让右边代码不执行

**==> picture [436 x 58] intentionally omitted <==**

- 原因：通过左边能得到整个式子的结果，因此没必要再判断右边

- 运算结果：无论 && 还是 || ，运算结果都是最后被执行的表达式值，一般用在变量赋值

**==> picture [467 x 82] intentionally omitted <==**

## 经常用于函数传递参数的完整性

**==> picture [395 x 140] intentionally omitted <==**

## if语句

- if语句有三种使用：单分支、双分支、多分支

- 单分支使用语法：

**==> picture [260 x 74] intentionally omitted <==**

- 括号内的条件为true时，进入大括号里执行代码

- 小括号内的结果若不是布尔类型时，会发生隐式转换转为布尔类型

双分支if语法：

**==> picture [253 x 155] intentionally omitted <==**

## 多分支if语法：

**==> picture [215 x 187] intentionally omitted <==**

- 先判断条件1，若满足条件1就执行代码1，其他不执行

- 若不满足则向下判断条件2，满足条件2执行代码2，其他不执行

- 若依然不满足继续往下判断，依次类推

- 若以上条件都不满足，执行else里的代码n

- 注：可以写N个条件，但这里演示只写2个

## switch语句

**==> picture [247 x 200] intentionally omitted <==**

- 找到跟小括号里数据全等的case值，并执行里面对应的代码

- 若没有全等的则执行default里的代码

- 例：数据若跟值2全等，则执行代码2

- break作用：立即结束所在的switch语句

- break可防止穿透，若不写会发生穿透效果

- 穿透效果：从一个case块进入到下一个case块

## 三元表达式

- 也是双分支，本质是if双分支的简写

- 符号：? 与 : 配合使用

- 语法：

**==> picture [344 x 44] intentionally omitted <==**

- 一般用来取值

- 案例：判断两个数谁是更大数

- 案例： 数字补0案例

## 术语解释

**==> picture [467 x 168] intentionally omitted <==**
