# 解决复杂同步任务导致线程阻塞

## 另起线程

参考：Web Workers

示例代码

独立worker文件

> 1 // worker.ts

> 2 self.addEventListener('message', (event: MessageEvent<any>) => {

> 3 // 执行复杂代码

> 4 self.postMessage('') // 传输执行结果

> 5 }) 6

> 7 // 主线程

> 8 const worker = new Worker(new URL('./worker.ts', import.meta.url).href) // 基于 vite 环境的引用路径

> 9 worker.postMessage('') // 执行开始

> 10 worker.addEventListener('message', (event: MessageEvent<any>) => {

> 11 // 执行结束

> 12 })

## Blob URL

> 1 const workerScript = `self.addEventListener('message', event => {

> 2 self.postMessage('')

> 3 })`

> 4 const blob = new Blob([workerScript])

- 5 const workerScriptURL = URL.createObjectURL(blob)

6

> 7 const worker = new Worker(workerScriptURL)

> 8 worker.postMessage('') // 执行开始

> 9 worker.addEventListener('message', (event: MessageEvent<any>) => {

> 10 // 执行结束

> 11 })

## 说明

使用window.postMessage与主线程通信 参数受到结构化克隆算法限制

worker内无法访问window（可用self代替）

worker内无法操作DOM

## 函数切片

参考： 生成器函数

示例代码

> 1 // 复杂代码

> 2 function* complexCalculate() {

> 3 let count = 0

> 4 const start = performance.now()

> 5 while (performance.now() - start <= 5000) {

> 6 yield count++

> 7 }

> 8 return count

> 9 } 10

> 11 // 将复杂代码切片为异步函数

> 12 function timeSlice(): Promise<number> {

> 13 return new Promise(resolve => {

> 14 const iteratorFn = complexCalculate()

> 15 const recursion = () => {

> 16 const start = performance.now()

> 17 let result: IteratorResult<unknown, number>

> 18 do {j

> 19 result = iteratorFn.next()

> 20 } while (!result.done && performance.now() - start < 16) // 切片为 16ms 长度的 任务

> 21 if (result.done) {

> 22 resolve(result.value)

> 23 } else {

> 24 requestAnimationFrame(recursion)

> 25 }

> 26 }

> 27 recursion()

> 28 })

> 29 }

## 说明

## 将原本的长任务切片成多个短任务

会降低执行效率，切片越小效率越低
