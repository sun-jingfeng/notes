# 对象类型题目

## 刷题网址

type-challenges

## 获取对象中的可选属性的联合类型

> 1 // Key extends keyof Obj 触发了分发

> 2 type OptionalKeys<Obj extends object, Key = keyof Obj> = Key extends keyof Obj

> 3 ? Omit<Obj, Key> extends Obj

> 4 ? Key

> 5 : never

> 6 : never 7

> 8 type T1 = OptionalKeys<{

> 9 foo: number | undefined

> 10 bar?: string

> 11 name?: string

> 12 flag: boolean

> 13 }>

> 14 // type T1 = "bar" | "name"

## 获取对象中的必有属性的联合类型

> 1 // Key extends keyof Obj 触发了分发

> 2 type RequiredKeys<Obj extends object, Key = keyof Obj> = Key extends keyof Obj

> 3 ? Omit<Obj, Key> extends Obj

> 4 ? never

> 5 : Key

> 6 : never 7

> 8 type T1 = RequiredKeys<{

> 9 foo: number | undefined

> 10 bar?: string

> 11 name?: string

> 12 flag: boolean

> 13 }>

> 14 // type T1 = "foo" | "flag"

## 判断是不是never类型

**==> picture [550 x 159] intentionally omitted <==**

**----- Start of picture text -----**<br>
1 type IsNever<T> = [T] extends [never] ? true : false<br>2<br>3 type T1 = IsNever<never> // true<br>4 type T2 = IsNever<string> // false<br>5 type T3 = IsNever<undefined> // false<br>6 type T4 = IsNever<any> // false<br>**----- End of picture text -----**<br>

## 判断是不是空对象类型

> 1 type IsEmptyObj<T> = keyof T extends never

> 2 ? unknown extends T // 特殊处理 unknown

> 3 ? false

> 4 : number extends T // 特殊处理 object ： {} 可以接受任何类型

> 5 ? true

> 6 : false

> 7 : false 8

> 9 type T1 = IsEmptyObj<string>

> 10 type T2 = IsEmptyObj<{ a: 1 }>

> 11 type T3 = IsEmptyObj<{}>

> 12 type T4 = IsEmptyObj<object>

> 13 type T5 = IsEmptyObj<any>

> 14 type T6 = IsEmptyObj<unknown>

> 15 type T7 = IsEmptyObj<never>

## 判断是不是any类型

> 1 type IsAny<T> = unknown extends T

> 2 ? [T] extends [number] // 特殊处理 any ，使用元组避免 any 分发

> 3 ? true

> 4 : false

> 5 : false

6 7 type T1 = IsAny<string> // false 8 type T2 = IsAny<any> // true 9 type T3 = IsAny<unknown> // false 10 type T4 = IsAny<never> // false

## 将联合类型变为交叉类型

## 正确答案（直接使用infer取参数类型）

**==> picture [550 x 329] intentionally omitted <==**

**----- Start of picture text -----**<br>
1 // T extends any 为了触发分发<br>2 type UnionToIntersection<T> = (T extends any ? (p: T) => void : never) extends (<br>3   p: infer P<br>4 ) => any<br>5   ? P<br>6   : never<br>7<br>8 type T1 = UnionToIntersection<{ a: string } | { b: number } | { c: boolean }><br>9 // type T1 = {<br>10 //   a: string;<br>11 // } & {<br>12 //   b: number;<br>13 // } & {<br>14 //   c: boolean;<br>15 // }<br>**----- End of picture text -----**<br>

## 错误答案（使用了内置类型Parameters或泛型传递后取参数）

> 1 type MyParameters<T> = T extends (p: infer P) => any ? P : never 2

> 3 type UnionToIntersection<T> = MyParameters<

> 4 T extends any ? (p: T) => void : never

> 5 > 6

> 7 type T1 = UnionToIntersection<{ a: string } | { b: number } | { c: boolean }>

> 8 // type T1 = {

> 9 //   a: string;

> 10 // } | {

> 11 //   b: number;

> 12 // } | {

> 13 //   c: boolean;

> 14 // }

## 将联合类型转化成元组类型

> 1 type FindUnionOne<T> = (

> 2 (T extends any ? (p: (p: T) => void) => void : never) extends (

> 3 p: infer P

> 4 ) => any

> 5 ? P

> 6 : never

> 7 ) extends (p: infer P) => any

> 8 ? P

> 9 : never 10

> 11 // 写法 1

> 12 type UnionToTuple<T, Result extends any[] = []> = [T] extends [never]

> 13 ? Result

> 14 : UnionToTuple<Exclude<T, FindUnionOne<T>>, [FindUnionOne<T>, ...Result]> 15

> 16 // 写法 2

> 17 type UnionToTuple2<T> = [T] extends [never]

> 18 ? []

> 19 : [...UnionToTuple<Exclude<T, FindUnionOne<T>>>, FindUnionOne<T>] 20

> 21 type T1 = UnionToTuple<1 | 2 | 3> // [1, 2, 3]

> 22 type T2 = UnionToTuple2<1 | 2 | 3> // [1, 2, 3]
