# 动态组件、插槽、自定义指令

## 什么是动态组件

   - 动态组件指的是动态切换组件的显示与隐藏。

- 如何实现动态组件渲染

vue 提供了一个内置的 <component> 组件，专门用来实现动态组件的渲染。示例代码如下：

**==> picture [435 x 256] intentionally omitted <==**

- 使用 keep-alive 保持状态

   - 默认情况下，切换动态组件时无法保持组件的状态。此时可以使用 vue 内置的 <keep-alive> 组件保持动态 组件的状态。示例代码如下：

**==> picture [320 x 78] intentionally omitted <==**

keep-alive 对应的生命周期函数

- 当组件被缓存时，会自动触发组件的 deactivated 生命周期函数。

- 当组件被激活时，会自动触发组件的 activated 生命周期函数。

**==> picture [377 x 174] intentionally omitted <==**

keep-alive 的 include 属性

include 属性用来指定：只有名称匹配的组件会被缓存。多个组件名之间使用英文的逗号分隔：

**==> picture [294 x 80] intentionally omitted <==**

## 插槽

什么是插槽

- 插槽（slot）是 vue 为组件的封装者提供的能力。允许开发者在封装组件时，把不确定的、希望由用户指定 的部分定义为插槽。

**==> picture [467 x 242] intentionally omitted <==**

可以把插槽认为是组件封装期间，为用户预留的内容的占位符。

## 体验插槽的基础用法

在封装组件时，可以通过 <slot> 元素定义插槽，从而为用户预留内容占位符。示例代码如下：

**==> picture [467 x 251] intentionally omitted <==**

没有预留插槽的内容会被丢弃

- 如果在封装组件时没有预留任何 <slot> 插槽，则用户提供的任何自定义内容都会被丢弃。示例代码如

下：

**==> picture [467 x 224] intentionally omitted <==**

## 后备内容

- 封装组件时，可以为预留的 <slot> 插槽提供后备内容（默认内容）。如果组件的使用者没有为插槽提 供任何内容，则后备内容会生效。示例代码如下：

**==> picture [307 x 124] intentionally omitted <==**

## 具名插槽

- 如果在封装组件时需要预留多个插槽节点，则需要为每个 <slot> 插槽指定具体的 name 名称。这种带有具 体名称的插槽叫做"具名插槽"。示例代码如下：

**==> picture [467 x 166] intentionally omitted <==**

## 为具名插槽提供内容

- 在向具名插槽提供内容的时候，我们可以在一个 <template> 元素上使用 v-slot 指令（只能在 template或组件上使用v-slot属性，在普通的例如div的元素上使用会报错），并以 v-slot 的参数的形 式提供其名称。示例代码如下：

**==> picture [239 x 274] intentionally omitted <==**

## 具名插槽的简写形式

跟 v-on 和 v-bind 一样，v-slot 也有缩写，即把参数之前的所有内容 (v-slot:) 替换为字符 #。例如 v- slot:header可以被重写为 #header：

**==> picture [233 x 275] intentionally omitted <==**

## 作用域插槽

- 在封装组件的过程中，可以为预留的 <slot> 插槽绑定 props 数据，这种带有 props 数据的 <slot> 叫做"作 用域插槽"。示例代码如下：

**==> picture [363 x 102] intentionally omitted <==**

- 使用作用域插槽

   - 可以使用 v-slot: 的形式，接收作用域插槽对外提供的数据。示例代码如下：

**==> picture [335 x 207] intentionally omitted <==**

- 解构插槽 Prop

   - 作用域插槽对外提供的数据对象，可以使用解构赋值简化数据的接收过程。示例代码如下：

**==> picture [467 x 218] intentionally omitted <==**

## 自定义指令

## 什么是自定义指令

vue 官方提供了 v-text、v-for、v-model、v-if 等常用的指令。除此之外 vue 还允许开发者自定义指令。

- 自定义指令的分类

   - vue 中的自定义指令分为两类，分别是：

      - 私有自定义指令

      - 全局自定义指令

- 私有自定义指令

   - 在每个 vue 组件中，可以在 directives 节点下声明私有自定义指令。示例代码如下：

**==> picture [345 x 210] intentionally omitted <==**

使用自定义指令

**==> picture [269 x 11] intentionally omitted <==**

**----- Start of picture text -----**<br>
在使用自定义指令时，需要加上 v- 前缀。示例代码如下：<br>**----- End of picture text -----**<br>

**==> picture [323 x 77] intentionally omitted <==**

- 为自定义指令动态绑定参数值

在 template 结构中使用自定义指令时，可以通过等号（=）的方式，为当前指令动态绑定参数值：

**==> picture [371 x 194] intentionally omitted <==**

通过 binding 获取指令的参数值

在声明自定义指令时，可以通过形参中的第二个参数，来接收指令的参数值：

**==> picture [422 x 190] intentionally omitted <==**

## update 函数

- bind 函数只调用 1 次：当指令第一次绑定到元素时调用，当 DOM 更新时 bind 函数不会被触发。 update 函数会在每次 DOM 更新时被调用。示例代码如下：

**==> picture [288 x 262] intentionally omitted <==**

## 函数简写

- 如果 insert 和update 函数中的逻辑完全相同，则对象格式的自定义指令可以简写成函数格式：

**==> picture [346 x 149] intentionally omitted <==**

## 全局自定义指令

- 全局共享的自定义指令需要通过"Vue.directive()"进行声明，示例代码如下：

**==> picture [385 x 127] intentionally omitted <==**
